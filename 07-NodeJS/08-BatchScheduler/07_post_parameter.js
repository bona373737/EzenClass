/**
 * @description nodeJs가 아닌 다른 언어로 백엔드를 구성한경우,
 * axios통신할때 parameter를 전달하는 방식이 다르다.
 * form-data객체를 활용 가상의 form태그를 생성하여
 * 프론트에서 form-input태그에 값을 입력하는것처럼 사용한다. 
 * 
 * 내장 객체인가???? 아님 설치 ㄱㄱ
 * npn install --save form-data
 */
import axios from 'axios';
import FormData from 'form-data';

(async()=>{
    let result= null;
    try {
        //
        
    } catch (error) {
        
    }
})();